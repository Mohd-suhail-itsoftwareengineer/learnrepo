- hosts: localhost
  connection: local
  become: yes

  tasks:

  - name: Deploy website
    copy:
      src: index.html
      dest: /var/www/html/index.html

  - name: Restart Apache
    service:
      name: apache2
      state: restarted
